import { CleanEmotionPipe } from './clean-emotion-pipe';

describe('CleanEmotionPipe', () => {
  it('create an instance', () => {
    const pipe = new CleanEmotionPipe();
    expect(pipe).toBeTruthy();
  });
});
